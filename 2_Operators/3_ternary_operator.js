/**
 * 
 * 
 * const variabelName = (condition) ? value1 : value2;
 */

let test1 = 70;
let test2 = 79;
const haspassedTest = (test1 >= 70 && test2 >= 79) ? "Yes" : "No";

const hasPassedtest2 = (test1 + test2 >= 140) ? "Yes" : "No";

console.log(`has passed test? ${haspassedTest}`);
